import { default as React } from 'react';
type RowConnectionLineProps = {
    currItemIdx: number;
    totalItemCount: number;
    lineDefinition: string;
};
export declare function InterRowConnectionLine({ currItemIdx, totalItemCount, lineDefinition }: RowConnectionLineProps): React.JSX.Element;
export {};
