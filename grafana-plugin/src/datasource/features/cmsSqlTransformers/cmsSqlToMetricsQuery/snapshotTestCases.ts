/* Those queries are used in order to test each of underlying components
 * separately but on the same set of data.
 * Refer to snapshot-testing.spec.ts in component directories
 */
export const queries = [
  'SELECT ORIGINNODE THRUNODE FROM O4SRV.INODESTS',
  "SELECT ORIGINNODE, MVSID FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2'; ",
  "SELECT ORIGINNODE, MVSID FROM KDP.REALTHDA AT('*HUB') WHERE ORIGINNODE = 'IC1A:RSD1:DB2';",
  "SELECT ORIGINNODE, MVSID FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' OR ORIGINNODE = 'ICA4:RSD1:DB2';",
  "SELECT AFFINITIES, EXPIRYTIME, GBLTMSTMP FROM O4SRV.INODESTS WHERE AFFINITIES <> '%IBM.CICSplex 00000000050000yA043';",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND THDXGETP <> 0;",
  "SELECT AFFINITIES, EXPIRYTIME, GBLTMSTMP FROM O4SRV.INODESTS WHERE AFFINITIES LIKE '%IBM.CICSplex 00000000050000yA043';",
  "SELECT KEY, AFFINITIES, EXPIRYTIME, GBLTMSTMP FROM O4SRV.INODESTS WHERE KEY LIKE 'IC??:RSD1:DB2';",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND TDIDASID >= 0;",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND 0 <= TDIDASID;",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND (TDIDASID > 160 OR THDXGETP > 0);",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND (TDIDASID + 1) > 0;",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND TDIDASID >= TDIDASID * 2 / 3;",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND THDXGETP <= (TDIDASID + THDXGETP);",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND (THDXGETP * 2) >= (TDIDASID /2) + 2;",
  'SELECT AFFINITIES, EXPIRYTIME, GBLTMSTMP FROM O4SRV.INODESTS WHERE FIRST(5);',
  "SELECT CMD FROM O4SRV.CCT WHERE CMD > '22' AND FIRST(5);",
  "SELECT ORIGINNODE, TIMESTAMP, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND SYSTEM.PARMA('IGNOREFILTER', 'r', 1);",
  "SELECT AFFINITIES, EXPIRYTIME, GBLTMSTMP FROM O4SRV.INODESTS WHERE SYSTEM.MATCH('LIKE', AFFINITIES, '*IBM.CICSplex*') = 0;",
  "SELECT AFFINITIES, EXPIRYTIME, GBLTMSTMP FROM O4SRV.INODESTS WHERE STRSCAN(AFFINITIES, 'HyQ') = 1;",
  'SELECT KEY, NAME, AFFINITIES FROM O4SRV.CCT WHERE EVENT("ZIBM_STATIC134_B4D8667934D54DA6_");',
  'SELECT DELTASTAT, RESULTS, ORIGINNODE, LCLTMSTMP FROM O4SRV.TADVISOR AT ( "HUB_WALDEVYSDED101" ) WHERE EVENT(DELTA("ZIBM_STATIC134_B4D8667934D54DA6_", "DELTAOR")) AND SYSTEM.PARMA("NODELIST", "IZSME:LZ", 8) AND SYSTEM.PARMA("ORIGINNODE", "IZSME:LZ", 8) AND SYSTEM.PARMA("IGNOREFILTER", "r", 1) AND SYSTEM.PARMA("CURRENTATTR", "Y", 1) AND (ORIGINNODE = "IZSME:LZ")',
  'SELECT NODE, ORIGINNODE, PRODUCT, O4ONLINE, AFFINITIES FROM O4SRV.INODESTS ORDER BY O4ONLINE DESC, PRODUCT;',
  "SELECT WRITETIME, MVSID, SSID, TDIDASID, THWTTOTW, THDXGETP64 FROM KDP.REALTHDA AT('SOME_NODE') HISTORY() WHERE ORIGINNODE = 'IC1A:RSD1:DB2' AND WRITETIME > 23423423;",
  "SELECT COUNT(ORIGINNODE) FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY ORIGINNODE;",
  "SELECT ORIGINNODE, THDXSTAT, COUNT(THDXSTAT), SUM(THDXSTAT) FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY THDXSTAT;",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT, COUNT(THDXSTAT), SUM(THDXSTAT) FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY THDXSTAT, QW0148CR;",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' HAVING THDXSTAT > 3",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' HAVING MIN(THDXSTAT) > 3",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY THDXSTAT HAVING MIN(QW0148CR) > 3",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY THDXSTAT ORDER BY THDXSTAT",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY THDXSTAT ORDER BY THDXSTAT DESC",
  "SELECT ORIGINNODE, MIN(QW0148CR), THDXSTAT FROM KDP.REALTHDA WHERE ORIGINNODE = 'IC1A:RSD1:DB2' GROUP BY THDXSTAT ORDER BY THDXSTAT DESC, QW0148EL ASC",
  'SElECT JCPUTM FROM KM5.ASCPUUTIL WHERE JCPUTM < 100',
  'SElECT JPROGRESS FROM KM5.ASCPUUTIL WHERE JPROGRESS = 0',
];
